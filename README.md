# Template Literals & Shorthand Assignment

This repository contains solutions for the JavaScript assignment focusing on:

- Template Literals  
- Object Property Shorthand  
- Method Shorthand  
- Arrow Function Shorthand (implicit return)

## 📁 Files Included
- `index.js` — JavaScript code for all three tasks (a, b, c)
- `README.md` — Project description

## 📝 Tasks

### a) Template Literal
Created variables `username` and `course` and printed a welcome message using a template string.

### b) Object Shorthand
Converted a regular object into shorthand syntax including shorthand properties and method.

### c) Arrow Function Shorthand
Created an arrow function `getFullName` using implicit return (no `return` keyword).

## 🚀 How to Run
Open the project folder in VS Code and run:

```sh
node index.js
